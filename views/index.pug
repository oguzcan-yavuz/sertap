doctype html
head
  meta(http-equiv='Content-Type' content='text/html; charset=utf-8')
  title Live input record and playback
  style(type='text/css').
    ul { list-style: none; }
    #recordingslist audio { display: block; margin-bottom: 10px; }
h1 Recorder.js simple WAV export example
p Make sure you are using a recent version of Google Chrome.
p
  | Also before you enable microphone input either plug in headphones or turn the volume down if you want to avoid ear splitting feedback!
button#record(type='button' name='record' onmousedown='startRecording(this);' onmouseup='stopRecording(this);') Listen
h2 Recordings
ul#recordingslist
script(src='/static/js/main.js')
script(src='/static/js/recorder.js')
